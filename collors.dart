import 'package:flutter/material.dart';

class MCollors{
  static const Color LPTop = Color.fromARGB(255, 221, 12, 5); // cor pagina de login gradiente superior
  static const Color LPBottom = Color.fromARGB(255, 1, 13, 185); // cor pagina de login gradiente inferior
  static const Color LPTopr = Color.fromARGB(255, 116, 116, 116); // cor pagina de register gradiente superior
  static const Color LPBottomr = Color.fromARGB(255, 39, 39, 39); // cor pagina de register gradiente inferior

}